(()=>{const d=document,$=s=>d.querySelector(s),$$=s=>[...d.querySelectorAll(s)];function lines(code){return code.split('\n').map(x=>`<span class="line">${x.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}</span>`).join('')}function enhance(){ $$('.code-wrap').forEach(w=>{const pre=$('.code',w);if(!pre||w.dataset.ready)return;w.dataset.ready='1';const raw=pre.textContent.replace(/^\n/,'').replace(/\n\s*$/,'');pre.innerHTML=lines(raw);const b=$('.code-copy',w);b?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(raw)}catch{const t=d.createElement('textarea');t.value=raw;d.body.appendChild(t);t.select();d.execCommand('copy');t.remove()}const old=b.textContent;b.textContent='Copied!';setTimeout(()=>b.textContent=old,1200)})})}$$('.mobile-menu').forEach(b=>b.onclick=()=>$('.doc-sidebar')?.classList.toggle('open'));$$('.doc-sidebar a').forEach(a=>a.addEventListener('click',()=>$('.doc-sidebar')?.classList.remove('open')));document.addEventListener('click',e=>{const fs=e.target.closest('[data-doc-font]');if(fs){const v=fs.dataset.docFont;d.documentElement.dataset.mbFont=v;$$('[data-doc-font]').forEach(x=>x.classList.toggle('active',x===fs));try{localStorage.setItem('mbrutals-font',v)}catch{}}});try{const f=localStorage.getItem('mbrutals-font');if(f)d.documentElement.dataset.mbFont=f}catch{}enhance()})();
