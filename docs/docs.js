document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.mb-code').forEach(box=>{const b=box.querySelector('.mb-copy');if(b)b.addEventListener('click',async()=>{const code=box.querySelector('pre')?.innerText||'';await navigator.clipboard.writeText(code);b.textContent='Copied!';setTimeout(()=>b.textContent='Copy',1000)})})});