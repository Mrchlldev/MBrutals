(function(window,document){'use strict';const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));const targetOf=(el)=>{const value=el&&(el.getAttribute('data-mb-target')||el.getAttribute('href'));if(!value||value==='#')return null;try{return $(value)}catch(e){return null}};const lock=(value)=>document.body.classList.toggle('mb-lock',!!value);function closeDropdowns(except){$$('.mb-dropdown.is-open').forEach(el=>{if(el!==except)el.classList.remove('is-open')});}function closeBackdropsIfEmpty(){const open=$('.mb-offcanvas.is-open,.mb-sidebar.is-open,.mb-modal.is-open');if(!open)lock(false);}function modalOpen(el){if(!el)return;el.classList.add('is-open');el.setAttribute('aria-hidden','false');lock(true);const focus=$('[data-mb-dismiss="modal"]',el)||$('button,input,select,textarea,a',el);if(focus)setTimeout(()=>focus.focus(),30);}function modalClose(el){if(!el)return;el.classList.remove('is-open');el.setAttribute('aria-hidden','true');closeBackdropsIfEmpty();}function panelOpen(el){if(!el)return;el.classList.add('is-open');el.setAttribute('aria-hidden','false');lock(true);}function panelClose(el){if(!el)return;el.classList.remove('is-open');el.setAttribute('aria-hidden','true');closeBackdropsIfEmpty();}function getBackdrop(){let backdrop=$('.mb-offcanvas-backdrop');if(!backdrop){backdrop=document.createElement('div');backdrop.className='mb-offcanvas-backdrop';document.body.appendChild(backdrop);backdrop.addEventListener('click',()=>{$$('.mb-offcanvas.is-open,.mb-sidebar.is-open').forEach(panelClose);});}return backdrop;}function sideOpen(el){if(!el)return;el.classList.add('is-open');el.setAttribute('aria-hidden','false');getBackdrop().classList.add('is-open');lock(true);}function sideClose(el){if(!el)return;el.classList.remove('is-open');el.setAttribute('aria-hidden','true');if(!$('.mb-offcanvas.is-open,.mb-sidebar.is-open')){const backdrop=$('.mb-offcanvas-backdrop');if(backdrop)backdrop.classList.remove('is-open');closeBackdropsIfEmpty();}}function toast(message,options={}){const stack=$('.mb-toast-stack')||(()=>{const el=document.createElement('div');el.className='mb-toast-stack';document.body.appendChild(el);return el;})();const item=document.createElement('div');item.className='mb-toast mb-toast-'+(options.type||'info');item.setAttribute('role','status');if(options.icon!==false){const icon=document.createElement('div');icon.className='mb-toast-icon';icon.textContent=options.icon||'✦';item.appendChild(icon);}const content=document.createElement('div');if(options.title){const title=document.createElement('strong');title.textContent=options.title;content.appendChild(title);}const text=document.createElement('div');text.textContent=String(message);content.appendChild(text);item.appendChild(content);const close=document.createElement('button');close.type='button';close.className='mb-toast-close';close.setAttribute('aria-label','Close');close.textContent='×';close.addEventListener('click',()=>item.remove());item.appendChild(close);stack.appendChild(item);if(options.duration!==0)setTimeout(()=>item.remove(),Number(options.duration)||4500);return item;}function carouselGo(carousel,index){if(!carousel)return;const track=$('.mb-carousel-track',carousel);const slides=$$('.mb-slide',carousel);if(!track||!slides.length)return;const next=(index+slides.length)%slides.length;carousel.dataset.mbIndex=String(next);track.style.transform='translateX(-'+(next*100)+'%)';$$('.mb-carousel-dot',carousel).forEach((dot,i)=>dot.classList.toggle('is-active',i===next));}function carouselMove(carousel,dir){carouselGo(carousel,Number(carousel?.dataset.mbIndex||0)+dir);}function copyText(text,button){const done=()=>{const old=button.textContent;button.textContent=button.getAttribute('data-mb-copied')||'Copied!';setTimeout(()=>button.textContent=old,1400);};if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(text).then(done).catch(()=>fallbackCopy(text,done));}else fallbackCopy(text,done);}function fallbackCopy(text,done){const area=document.createElement('textarea');area.value=text;area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.select();try{document.execCommand('copy');done()}finally{area.remove()}}document.addEventListener('click',(event)=>{const toggle=event.target.closest('[data-mb-toggle]');if(toggle){const type=toggle.getAttribute('data-mb-toggle');const target=targetOf(toggle);if(type==='modal'){event.preventDefault();modalOpen(target)}if(type==='offcanvas'||type==='sidebar'){event.preventDefault();const panel=target||toggle.closest('.mb-offcanvas,.mb-sidebar');if(panel){if(panel.classList.contains('is-open'))sideClose(panel);else sideOpen(panel);}}if(type==='dropdown'){event.preventDefault();const dropdown=toggle.closest('.mb-dropdown');closeDropdowns(dropdown);if(dropdown)dropdown.classList.toggle('is-open');}if(type==='navbar'||type==='collapse'){event.preventDefault();const nav=toggle.closest('.mb-navbar');const targetNav=target||(nav&&$('.mb-navbar-nav',nav));if(nav)nav.classList.toggle('mb-is-open');if(targetNav)targetNav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(nav?.classList.contains('mb-is-open')));}if(type==='accordion'){event.preventDefault();const item=toggle.closest('.mb-accordion-item');if(item){const parent=item.parentElement;if(!toggle.closest('.mb-accordion')?.hasAttribute('data-mb-multiple')){$$('.mb-accordion-item.is-open',parent).forEach(other=>{if(other!==item)other.classList.remove('is-open')});}item.classList.toggle('is-open');}}if(type==='tab'){event.preventDefault();const tabs=toggle.closest('.mb-tabs');if(tabs){$$('.mb-tab',tabs).forEach(tab=>tab.classList.remove('is-active'));$$('.mb-tab-panel',tabs).forEach(panel=>panel.classList.remove('is-active'));toggle.classList.add('is-active');if(target)target.classList.add('is-active');}}}const dismiss=event.target.closest('[data-mb-dismiss]');if(dismiss){const type=dismiss.getAttribute('data-mb-dismiss');if(type==='modal')modalClose(dismiss.closest('.mb-modal'));if(type==='offcanvas'||type==='sidebar')sideClose(dismiss.closest('.mb-offcanvas,.mb-sidebar'));}if(event.target.classList.contains('mb-modal'))modalClose(event.target);if(!event.target.closest('.mb-dropdown'))closeDropdowns();const prev=event.target.closest('[data-mb-carousel-prev]');const next=event.target.closest('[data-mb-carousel-next]');if(prev)carouselMove(prev.closest('.mb-carousel'),-1);if(next)carouselMove(next.closest('.mb-carousel'),1);const dot=event.target.closest('[data-mb-carousel-to]');if(dot)carouselGo(dot.closest('.mb-carousel'),Number(dot.getAttribute('data-mb-carousel-to')));const copy=event.target.closest('[data-mb-copy]');if(copy)copyText(copy.getAttribute('data-mb-copy')||'',copy);const font=event.target.closest('[data-mb-font-switch]');if(font){const value=font.getAttribute('data-mb-font-switch');document.documentElement.setAttribute('data-mb-font',value);$$('.mb-font-switch').forEach(btn=>btn.classList.toggle('is-active',btn===font));try{localStorage.setItem('mbrutals-font',value)}catch(e){}}const theme=event.target.closest('[data-mb-theme-switch]');if(theme){const value=theme.getAttribute('data-mb-theme-switch');document.documentElement.setAttribute('data-mb-theme',value);try{localStorage.setItem('mbrutals-theme',value)}catch(e){}}});document.addEventListener('keydown',(event)=>{if(event.key==='Escape'){$$('.mb-modal.is-open').forEach(modalClose);$$('.mb-offcanvas.is-open,.mb-sidebar.is-open').forEach(sideClose);closeDropdowns();}});window.addEventListener('load',()=>{let font=null,theme=null;try{font=localStorage.getItem('mbrutals-font');theme=localStorage.getItem('mbrutals-theme')}catch(e){}if(font)document.documentElement.setAttribute('data-mb-font',font);if(theme)document.documentElement.setAttribute('data-mb-theme',theme);$$('.mb-tabs').forEach(tabs=>{const active=$('.mb-tab.is-active',tabs)||$('.mb-tab',tabs);if(active){active.classList.add('is-active');const panel=targetOf(active);if(panel)panel.classList.add('is-active');}});$$('.mb-carousel').forEach(carousel=>{carouselGo(carousel,Number(carousel.dataset.mbIndex||0));const autoplay=Number(carousel.getAttribute('data-mb-autoplay'));if(autoplay>0)setInterval(()=>carouselMove(carousel,1),autoplay);});$$('.mb-modal[aria-hidden="false"],.mb-offcanvas[aria-hidden="false"],.mb-sidebar[aria-hidden="false"]').forEach(el=>el.setAttribute('aria-hidden','true'));});window.MBrutals={version:'1.3.0',toast,modal:{open:modalOpen,close:modalClose},offcanvas:{open:sideOpen,close:sideClose},sidebar:{open:sideOpen,close:sideClose},carousel:{go:carouselGo,next:el=>carouselMove(el,1),prev:el=>carouselMove(el,-1)}};})(window,document);